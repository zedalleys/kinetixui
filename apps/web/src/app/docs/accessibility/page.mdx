export const metadata = {
  title: "Accessibility",
  description: "How KinetixUI components meet WCAG, and the known trade-offs.",
};

# Accessibility

Accessibility is a build constraint here, not a review step. Every interactive
component is a [Radix Primitive](https://www.radix-ui.com/primitives) underneath,
so keyboard interaction, focus management, and ARIA roles come correct by
default — KinetixUI only styles them against the token contract.

## What you get

- **Keyboard** — every menu, dialog, tab set, combobox, and disclosure is fully
  operable without a mouse (arrow keys, `Home`/`End`, `Esc`, type-ahead, focus
  trapping in overlays). This is Radix behaviour, preserved as-is.
- **Focus visibility** — most components draw a **2px `--ring` outline** on
  `:focus-visible` (checkbox, radio, switch, list rows, menu items, close
  buttons, nav). `--ring` clears **8.8:1** against the page in dark mode and
  **11.6:1** in light — well past the 3:1 that WCAG 2.2
  [SC 1.4.11](https://www.w3.org/WAI/WCAG22/Understanding/non-text-contrast)
  asks for. Form controls (Button, Input, Select, Textarea, Fab, NumberInput,
  InputGroup, FileUpload) instead use the `--shadow-focus` composite token,
  which now carries a real dark set (`tokens/semantic/shadow.dark.json` →
  `@kinetixui/tokens/css/extras/dark`), so the ring clears **8.8:1** in dark
  too — not just on this site.
- **Text contrast** — every foreground/surface pair components actually render
  is audited by [`scripts/check-contrast.mjs`](https://github.com/zedalleys/kinetixui/blob/main/scripts/check-contrast.mjs)
  (`pnpm check:contrast`, run in CI). Every pair meets **WCAG AA (4.5:1)** in
  both themes, with two documented exceptions on the light `--warning` orange
  (see below). CI fails the build if a new pair regresses.
- **Labels** — icon-only controls (`Button size="icon"`, `Fab`, close buttons)
  require an `aria-label`; the components don't render without one where it
  matters.
- **Reduced motion** — transitions are short and `transition-colors` only;
  nothing animates position or opacity on a loop.

## The one trade-off

On the darkest surface, a **subtle hover fill can't reach 3:1 on its own**.
`--accent` (the hover wash used by ghost/outline buttons, menu items, list rows)
sits at ~1.2:1 against `--background` in dark mode — visible, but below the
non-text-contrast bar if it were the *only* cue.

So components never make it the only cue — the highlighted / hovered state also
draws a `--ring` inset outline (8.8:1 dark / 11.6:1 light):

- **Ghost / Outline buttons** on `:hover`.
- **Menu items** (dropdown, context, menubar, select) and **interactive list
  rows** on `:focus` / `:hover`.
- **Toggle** and menubar triggers in their pressed / open state.
- Everything is also keyboard-reachable, where `:focus-visible` draws the same
  ring.

If you retheme, keep `--ring` high-contrast against `--background` and
`--foreground` in both modes — the focus outline depends on it. Run
`pnpm check:contrast` after changing the palette.

## Known gaps (tracked)

From the platform accessibility audit (`ACCESSIBILITY-AUDIT.md`):

- **`--warning` on light** — the orange (`#f97907`) is **2.7:1** as text on the
  page and **2.6:1** in the `Tag` warning variant. It needs a design call on a
  darker value; `check:contrast` allow-lists these two pairs and still fails on
  anything new. Use `--warning` for icons and large text only until then.
- **~~`--shadow-focus` has no dark variant~~** — fixed. `shadow.dark.json` now
  feeds a `.dark { --shadow-focus* }` block into `extras.dark.css` /
  `registry/kinetixui/globals.css`; the dark ring clears 8.8:1. Remaining:
  `check-contrast.mjs` doesn't yet assert the four `--shadow-focus*` composites.
- **`--shadow-focus` was the light-mode `destructive` fill too** — the Figma
  `error` value `#ec5047` failed AA (3.3:1). `--destructive` on light is now
  `red.500` (`#c60a0a`, 5.6–6.1:1); it no longer tracks the Figma error token.
