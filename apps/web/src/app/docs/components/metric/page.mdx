export const metadata = {
  title: "Metric",
  description: "A stat / KPI display — label, value, an optional trend indicator and an optional chart or icon slot.",
};

# Metric

A stat / KPI display — label, value, an optional trend indicator and an optional chart or icon slot.

<ComponentPreview name="metric-demo" />

## Installation

```bash
npx @kinetixui/cli add metric
```

## Usage

```tsx
import { Metric } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
