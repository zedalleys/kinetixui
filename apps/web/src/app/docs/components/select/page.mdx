export const metadata = {
  title: "Select",
  description: "Displays a list of options for the user to pick from — triggered by a button. Reconciled 1:1 with design source node 54855:13882.",
};

# Select

Displays a list of options for the user to pick from — triggered by a button. Reconciled 1:1 with design source node 54855:13882.

<ComponentPreview name="select-demo" />

## Installation

```bash
npx kinetixui add select
```

## Usage

```tsx
import { Select, SelectTrigger, SelectValue, SelectContent, SelectItem } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
