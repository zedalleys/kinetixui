export const metadata = {
  title: "Comparison Slider",
  description: "A drag handle wiping between two stacked layers — a before/after image, a redesign preview.",
};

# Comparison Slider

<ComponentMeta />

A drag handle wiping between two stacked layers — a before/after image, a redesign preview.

<ComponentPreview name="comparison-slider-demo" />

## Installation

```bash
npx @kinetixui/cli add comparison-slider
```

## Usage

```tsx
import { ComparisonSlider } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
