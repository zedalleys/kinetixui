export const metadata = {
  title: "Image",
  description: "A ratio-locked image with a muted loading placeholder and an error fallback. Ported 1:1 from design source — Capi \"Image\" (ratios 1:1 / 3:2 / 4:3 / 3:4 / 3:1 / 16:9).",
};

# Image

A ratio-locked image with a muted loading placeholder and an error fallback. Ported 1:1 from design source — Capi "Image" (ratios 1:1 / 3:2 / 4:3 / 3:4 / 3:1 / 16:9).

<ComponentPreview name="image-demo" />

## Installation

```bash
npx shadcn@latest add https://kinetixui.com/r/image.json
```

## Usage

```tsx
import { Image } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
