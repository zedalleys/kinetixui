export const metadata = {
  title: "Navigation Bar",
  description: "A mobile top app bar — back button, title, actions.",
};

# Navigation Bar

<ComponentMeta />

A mobile top app bar — back button, title, actions.

<ComponentPreview name="navigation-bar-demo" />

## Installation

```bash
npx @kinetixui/cli add navigation-bar
```

## Usage

```tsx
import { NavigationBar } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
