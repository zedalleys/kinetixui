export const metadata = {
  title: "Tour",
  description: "Sequenced spotlight popovers over real elements (onboarding walkthroughs), with dismiss/skip/next.",
};

# Tour

<ComponentMeta />

Sequenced spotlight popovers over real elements (onboarding walkthroughs), with dismiss/skip/next.

<ComponentPreview name="tour-demo" />

## Installation

```bash
npx @kinetixui/cli add tour
```

## Usage

```tsx
import { Tour } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
