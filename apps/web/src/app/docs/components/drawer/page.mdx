export const metadata = {
  title: "Drawer",
  description: "A drawer component for React, built on Vaul.",
};

# Drawer

A drawer component for React, built on Vaul.

<ComponentPreview name="drawer-demo" />

## Installation

```bash
npx @kinetixui/cli add drawer
```

## Usage

```tsx
import { Drawer, DrawerTrigger, DrawerContent, DrawerHeader, DrawerTitle, DrawerFooter } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
