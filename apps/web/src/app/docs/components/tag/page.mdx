export const metadata = {
  title: "Tag",
  description: "A container-tinted, dismissible chip. Reconciled 1:1 with design source node 54855:14021 (variants: default | secondary | destructive | warning | outline).",
};

# Tag

A container-tinted, dismissible chip. Reconciled 1:1 with design source node 54855:14021 (variants: default | secondary | destructive | warning | outline).

<ComponentPreview name="tag-demo" />

## Installation

```bash
npx kinetixui add tag
```

## Usage

```tsx
import { Tag, tagVariants } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
