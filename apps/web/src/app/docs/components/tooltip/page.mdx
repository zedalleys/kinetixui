export const metadata = {
  title: "Tooltip",
  description: "A popup that displays information related to an element on focus / hover.",
};

# Tooltip

A popup that displays information related to an element on focus / hover.

<ComponentPreview name="tooltip-demo" />

## Installation

```bash
npx shadcn@latest add https://kinetixui.com/r/tooltip.json
```

## Usage

```tsx
import { Tooltip, TooltipProvider, TooltipTrigger, TooltipContent } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
