export const metadata = {
  title: "Quote",
  description: "A blockquote with an optional attributed author (name, title, avatar).",
};

# Quote

<ComponentMeta />

A blockquote with an optional attributed author (name, title, avatar).

<ComponentPreview name="quote-demo" />

## Installation

```bash
npx @kinetixui/cli add quote
```

## Usage

```tsx
import { Quote } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
