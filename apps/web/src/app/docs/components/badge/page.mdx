export const metadata = {
  title: "Badge",
  description: "A solid pill status marker. Reconciled 1:1 with design source node 54855:13995 (variants: default | secondary | destructive | outline | subtle).",
};

# Badge

A solid pill status marker. Reconciled 1:1 with design source node 54855:13995 (variants: default | secondary | destructive | outline | subtle).

<ComponentPreview name="badge-demo" />

## Installation

```bash
npx kinetixui add badge
```

## Usage

```tsx
import { Badge } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config.
