export const metadata = {
  title: "Data Table",
  description: "Powerful table and datagrid built with TanStack Table.",
};

# Data Table

<ComponentMeta />

Powerful table and datagrid built with TanStack Table.

<ComponentPreview name="data-table-demo" />

## Installation

```bash
npx @kinetixui/cli add data-table
```

## Usage

```tsx
import { DataTable, Table } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
