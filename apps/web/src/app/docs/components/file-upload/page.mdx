export const metadata = {
  title: "File Upload",
  description: "A drag-and-drop zone plus file list, single or multiple.",
};

# File Upload

A drag-and-drop zone plus file list, single or multiple.

<ComponentPreview name="file-upload-demo" />

## Installation

```bash
npx @kinetixui/cli add file-upload
```

## Usage

```tsx
import { FileUpload, FileUploadItem } from "@kinetixui/ui";
```

Styled entirely from the KinetixUI token contract — `bg-primary`,
`text-muted-foreground`, `border-input`, `ring-ring`. Works in light and dark
with no extra config, and keyboard + ARIA behaviour comes from Radix — see
[Accessibility](/docs/accessibility).
