export const metadata = {
  title: "CLI",
  description: "Add components with the kinetixui CLI.",
};

# CLI

The `kinetixui` CLI drops component source straight into your project — no
account, no build step to consume it, just files you own.

## init

Set up `kinetixui.json` (your aliases) and pull in the token contract:

```bash
npx @kinetixui/cli init
```

You'll be asked where your global CSS file lives and which import aliases to
use for components and utils. Pass `-y` to accept the defaults and skip the
prompts.

## add

```bash
npx @kinetixui/cli add button
```

Add more than one at once:

```bash
npx @kinetixui/cli add button card dialog
```

`add` resolves registry dependencies automatically — asking for `button`
also pulls in `tokens` the first time, installs the npm packages it needs
(`@radix-ui/react-slot`, `class-variance-authority`, …) with whichever
package manager your project already uses (pnpm / yarn / bun / npm, detected
from the lockfile), and writes the component into your configured `ui`
alias directory. Existing files are left alone unless you pass
`--overwrite`.

Want everything at once instead of naming each one?

```bash
npx @kinetixui/cli add --all
```

## list

Not sure what's available? List every component in the registry:

```bash
npx @kinetixui/cli list
```

## inspect

Look at one component before (or after) adding it — description, npm
dependencies, other registry items it needs, the files it writes, and
whether it's already installed in the current project:

```bash
npx @kinetixui/cli inspect button
```

For components built on a variant matrix (`Button`, `Badge`, `Sheet`, …),
`inspect` also prints their variant axes and each axis's option names —
`Primary`/`Secondary`/`Outline`/… for `variant`, `sm`/`md`/`lg`/… for
`size`, and so on — pulled from a generated `specs/components/<name>.json`
contract manifest (source: the component's own `cva()` definition, so it
can't drift from the real prop surface). Not every component has one yet —
today it covers the ~20 components with a real variant-axis matrix; the
rest (composite/structural components like `Card` or `Dialog`) don't print
a Variants section. See [Component specs](/docs/component-specs).

## parity

See which native platforms carry a component — or every component at once:

```bash
npx @kinetixui/cli parity button card kanban-board
npx @kinetixui/cli parity          # every component in the registry
```

Prints a matrix (`✓`/`—` per platform, plus a status tag for anything
`beta`/`deprecated`) straight from the registry index, so it stays in
step with [platform-parity.json](https://github.com/zedalleys/kinetixui/blob/main/platform-parity.json)
and [component-status.json](https://github.com/zedalleys/kinetixui/blob/main/component-status.json)
without a second copy of that data anywhere.

## doctor

Sanity-check a project that's already run `init`: is `kinetixui.json`
valid, do its aliases resolve to real directories, does the Tailwind CSS
target exist, is the registry reachable, and does everything sitting in
your `ui` directory still match a name the registry knows about. Each
check prints pass / warn / fail; `doctor` exits non-zero if anything
failed outright (not just warned), so it's safe to run in CI:

```bash
npx @kinetixui/cli doctor
```

## Options

| Flag | Applies to | Does |
| --- | --- | --- |
| `-y, --yes` | `init` | skip prompts, use defaults |
| `-a, --all` | `add` | install every component in the registry |
| `-o, --overwrite` | `add` | replace files that already exist |
| `-r, --registry <url>` | `init`, `add`, `list`, `inspect`, `parity` | point at a different registry (defaults to `https://kinetixui.com/r`) |

`doctor` always checks against the default registry — it doesn't take
`--registry` yet, since `kinetixui.json` doesn't record which registry a
project was set up against.

See [kinetixui.json](/docs/kinetixui-json) for what the config file controls.
