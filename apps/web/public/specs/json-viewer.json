{
  "$schema": "https://kinetixui.com/schema/component-spec.json",
  "name": "json-viewer",
  "title": "Json Viewer",
  "status": "stable",
  "since": "0.12.0",
  "platforms": [
    "React",
    "SwiftUI",
    "Compose",
    "Flutter"
  ],
  "variants": {},
  "components": [
    {
      "name": "JsonViewer",
      "props": [
        {
          "name": "data",
          "type": "unknown",
          "required": true
        },
        {
          "name": "expandDepth",
          "type": "number",
          "required": false,
          "description": "tree depth (root = 0) to auto-expand; deeper nodes start collapsed. Default 1."
        },
        {
          "name": "copyable",
          "type": "boolean",
          "required": false,
          "description": "show the copy-to-clipboard button. Defaults to true. Replaces `hideCopy`, which was a negative boolean: `hideCopy={false}` read as a double negative, and the prop's default behaviour could not be sta…"
        }
      ]
    }
  ],
  "source": "packages/ui/src/components/json-viewer.tsx"
}
