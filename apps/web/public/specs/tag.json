{
  "$schema": "https://kinetixui.com/schema/component-spec.json",
  "name": "tag",
  "title": "Tag",
  "status": "stable",
  "since": "0.4.1",
  "platforms": [
    "React",
    "Angular",
    "SwiftUI",
    "Compose",
    "Flutter"
  ],
  "variants": {
    "variant": [
      "default",
      "secondary",
      "destructive",
      "warning",
      "outline"
    ]
  },
  "components": [
    {
      "name": "Tag",
      "props": [
        {
          "name": "onRemove",
          "type": "(() => void)",
          "required": false,
          "description": "show a dismiss button; called when it's clicked"
        },
        {
          "name": "removeLabel",
          "type": "string",
          "required": false,
          "description": "Accessible name for the dismiss button. Defaults to `Remove <tag text>` when the tag's children are a plain string, so a row of tags does not become a row of identical \"Remove\" buttons. Pass this whe…"
        },
        {
          "name": "asChild",
          "type": "boolean",
          "required": false,
          "description": "render as the single child element (Radix Slot) instead of <span> — e.g. `<Tag asChild><a href=\"/filter\">Active</a></Tag>` for a linked tag. Combined with `onRemove`, the remove button nests inside t…"
        },
        {
          "name": "variant",
          "type": "\"default\" | \"destructive\" | \"warning\" | \"secondary\" | \"outline\" | null",
          "required": false
        }
      ]
    }
  ],
  "source": "packages/ui/src/components/tag.tsx"
}
