---
"@kinetixui/cli": patch
---

`kinetixui parity` now says its list is registry items only. A few manifest components — ones that ship inside another item, or are documented compositions — have no registry entry of their own, so the "N of N shown" line is smaller than the component total on kinetixui.com. The note carries no count of its own and links to `/docs/platforms` for full coverage.
